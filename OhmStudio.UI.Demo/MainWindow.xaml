<CustomChromeWindow x:Class="OhmStudio.UI.Demo.MainWindow"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:local="clr-namespace:OhmStudio.UI.Demo"
                    xmlns:control="http://ohmstudio.ui.com"
                    d:DataContext="{d:DesignInstance Type=local:MainWindow}"
                    xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
                    xmlns:pt="http://propertytools.org/wpf"
                    xmlns:oxy="http://oxyplot.org/wpf"
                    xmlns:hc="https://handyorg.github.io/handycontrol"
                    mc:Ignorable="d"
                    IsShowTitle="True"
                    IsShowIcon="True"
                    Icon="/icon.ico"
                    WindowStartupLocation="CenterScreen"
                    ResizeMode="CanResizeWithGrip"
                    UseLayoutRounding="True"
                    SnapsToDevicePixels="True"
                    Title="{x:Static local:App.Name}"
                    Height="720"
                    Width="1280">

    <CustomChromeWindow.RightWindowCommands>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" >
            <Border IsHitTestVisible="False" VerticalAlignment="Center">
                <Border.Style>
                    <Style TargetType="Border">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsActive,RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                                <Setter Property="Background" Value="{DynamicResource SystemColorsWindowPanel}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <TextBlock Margin="4"
                           HorizontalAlignment="Center"
                           FontWeight="Bold"
                           Text="{Binding Title,RelativeSource={RelativeSource AncestorType=Window}}" >
                    <TextBlock.Style>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsActive,RelativeSource={RelativeSource AncestorType=Window}}" Value="False">
                                    <Setter Property="Foreground" Value="{DynamicResource EnvironmentMainWindowInactiveCaptionText}" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Border>
            <ComboBox ItemsSource="{Binding FontSizeList}"
                      SelectedItem="{Binding CurrentFontSize}"
                      Margin="6,0"/>
            <ComboBox ItemsSource="{Binding FontFamilyList}"
                      SelectedItem="{Binding CurrentFontFamily}"/>
            <ComboBox DisplayMemberPath="Name"
                      Margin="6,0"
                      ItemsSource="{x:Static local:OhmhemeCollection.AllThemes}"
                      SelectedItem="{Binding CurrentTheme}"/>
        </StackPanel>

    </CustomChromeWindow.RightWindowCommands>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DockingManager x:Name="dockingManager" >

            <LayoutRoot>
                <LayoutPanel Orientation="Horizontal">
                    <LayoutDocumentPaneGroup>
                        <LayoutDocumentPane  >
                            <LayoutDocument Title="Button">
                                <UniformGrid>
                                    <Button Click="Button_Click">Click Me</Button>
                                    <Button Click="Button_Click_1">Button</Button>
                                    <Button Click="Button_Click_2">GC</Button>
                                </UniformGrid>

                            </LayoutDocument>
                            <LayoutDocument Title="DataGrid">
                                <DataGrid x:Name="da">

                                </DataGrid>
                            </LayoutDocument>
                            <LayoutDocument Title="RollBox">
                                <!--<hc:Carousel>
                                     添加轮播项 
                                    <hc:CarouselItem>
                                         轮播项内容 
                                    </hc:CarouselItem>
                                    <hc:CarouselItem>
                                        <Image Source="icon.ico"/>
                                         轮播项内容 
                                    </hc:CarouselItem>
                                     可以添加更多轮播项 
                                </hc:Carousel>-->
                                <RollBox>
                                    <RollBox.Items>
                                        <Image Source="download.jpg"/>
                                        <Image Source="icon.ico"/>
                                    </RollBox.Items>
                                </RollBox>
                            </LayoutDocument>
                            <LayoutDocument Title="PlotView">
                                <oxy:PlotView Model="{Binding PlotModel}" Background="#2e2e2e" Foreground="Red"/>
                            </LayoutDocument>
                            <LayoutDocument Title="TextEditor">
                                <TabControl>
                                    <TabItem Header="XML">
                                        <avalonEdit:TextEditor SyntaxHighlighting="XML" ShowLineNumbers="True" >

                                        </avalonEdit:TextEditor>
                                    </TabItem>
                                    <TabItem Header="C#">
                                        <avalonEdit:TextEditor SyntaxHighlighting="C#" ShowLineNumbers="True" >

                                        </avalonEdit:TextEditor>
                                    </TabItem>
                                    <TabItem Header="C++">
                                        <avalonEdit:TextEditor SyntaxHighlighting="C++" ShowLineNumbers="True"></avalonEdit:TextEditor>
                                    </TabItem>
                                </TabControl>
                            </LayoutDocument>
                            <LayoutDocument Title="Other" IsSelected="True">
                                <WrapPanel Orientation="Vertical" Name="wr"  >
                                    <SearchBar Margin="10" VerticalAlignment="Top" Width="400"/>
                                    <TextBox Width="400" VerticalAlignment="Top" Name="te" Text="{Binding CurrentDateTime,Mode=TwoWay}"/>
                                    <PasswordBox Margin="10" Width="400"  VerticalAlignment="Top"/>
                                    <PasswordBoxControl Name="ps" Width="400" VerticalAlignment="Top" />
                                    <PasswordBoxControl Margin="10" Width="400" VerticalAlignment="Top"/>
                                    <DateTimePicker VerticalAlignment="Top" Width="400" DateTime="{Binding CurrentDateTime,Mode=TwoWay}"/>
                                    <ComboBox Margin="10" Width="400" ItemsSource="{Binding FileNodes}" DisplayMemberPath="Name" VerticalAlignment="Top" />
                                    <ComboBox Width="400" ItemsSource="{Binding FileNodes}" DisplayMemberPath="Name" VerticalAlignment="Top" IsEditable="True"/>
                                    <CheckComboBox  Margin="10"  Width="400" ItemsSource="{Binding FileNodes}" VerticalAlignment="Top" SelectedItems="{Binding SelectedItemsFileNodes,Mode=TwoWay}"/>
                                    <Button Click="Button_Click_4">显示选中项</Button>
                                </WrapPanel>

                            </LayoutDocument>
                            <LayoutDocument Title="ProgressBar">
                                <ProgressBar Value="50" IsIndeterminate="True"></ProgressBar>
                            </LayoutDocument>
                            <LayoutDocument Title="MultiSelectComboBox">
                                <StackPanel Orientation="Horizontal">
                                    <hc:CheckComboBox   ItemsSource="{Binding FileNodes}" VerticalAlignment="Top" />
                                    <ComboBox DisplayMemberPath="Name"  ItemsSource="{Binding FileNodes}" VerticalAlignment="Top" IsEditable="True"/>
                                    <CheckComboBox DisplayMemberPath="Name"  ItemsSource="{Binding FileNodes}" VerticalAlignment="Top" />
                                    <CheckBox IsChecked="{x:Null}" VerticalAlignment="Top" />
                                    <ComboBox ItemsSource="{Binding FileNodes}" DisplayMemberPath="Name" VerticalAlignment="Top" />
                                    <ComboBox DisplayMemberPath="Name" ItemsSource="{Binding FileNodes}" VerticalAlignment="Top" />
                                    <TextBox Width="400" VerticalAlignment="Top" Text="{Binding CurrentDateTime}"/>
                                </StackPanel>

                            </LayoutDocument>
                        </LayoutDocumentPane>
                    </LayoutDocumentPaneGroup>
                    <LayoutAnchorablePaneGroup DockWidth="400">
                        <LayoutAnchorablePane>
                            <LayoutAnchorable Title="ListView">
                                <ListView Name="li" >

                                    <ListView.View>
                                        <GridView>
                                            <GridViewColumn Header="Time" DisplayMemberBinding="{Binding Time}"/>
                                            <GridViewColumn Header="V0" DisplayMemberBinding="{Binding V0}"/>
                                            <GridViewColumn Header="V1" DisplayMemberBinding="{Binding V1}"/>
                                            <GridViewColumn Header="RX" DisplayMemberBinding="{Binding RX}"/>
                                        </GridView>
                                    </ListView.View>
                                    <ListView.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="Zxc"></MenuItem>
                                        </ContextMenu>
                                    </ListView.ContextMenu>
                                </ListView>
                            </LayoutAnchorable>
                            <LayoutAnchorable Title="ListBox">
                                <ListBox>
                                    <ListBoxItem>123</ListBoxItem>
                                    <ListBoxItem>456</ListBoxItem>
                                    <ListBoxItem>789</ListBoxItem>
                                </ListBox>
                            </LayoutAnchorable>
                            <LayoutAnchorable Title="PropertyGrid">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <PropertyGrid SelectedObject="{Binding Items}"/>
                                    <Button Click="Button_Click_3" Grid.Row="1">Click</Button>
                                    <!--<hc:CheckComboBox  Grid.Row="2">Click</hc:CheckComboBox>-->
                                </Grid>

                            </LayoutAnchorable>
                        </LayoutAnchorablePane>
                    </LayoutAnchorablePaneGroup>
                </LayoutPanel>
            </LayoutRoot>
        </DockingManager>
        <StatusBar Grid.Row="1">
            <StatusBarItem>Ready</StatusBarItem>
        </StatusBar>
    </Grid>
</CustomChromeWindow> 